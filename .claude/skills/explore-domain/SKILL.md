---
name: explore-domain
description: Produce a read-only, evidence-backed current-state map of a Travel domain module. Use when asked what a module contains, implements, tests, or still lacks.
---

Resolve the Git repository root, then read and follow `.agents/skills/explore-domain/SKILL.md` from that root as the canonical workflow.
