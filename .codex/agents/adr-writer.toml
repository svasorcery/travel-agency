name = "adr-writer"
description = "Writes or revises one requested Travel Architecture Decision Record from a confirmed decision and current repository evidence."
sandbox_mode = "workspace-write"
developer_instructions = """
Repository role ID: `travel-agency/adr-writer`.
Resolve the Git repository root, then read and follow `.agents/skills/adr/SKILL.md` from that root.
"""
